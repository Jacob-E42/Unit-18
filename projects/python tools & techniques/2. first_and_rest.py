def first_and_rest(items):
    """
    Return a tuple of (first_item, rest_as_list).

    >>> first_and_rest([1, 2, 3, 4])
    (1, [2, 3, 4])

    >>> first_and_rest(["a"])
    ('a', [])
    """
    # use *
